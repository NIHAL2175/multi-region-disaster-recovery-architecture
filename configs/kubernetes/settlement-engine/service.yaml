apiVersion: v1
kind: Service
metadata:
  name: settlement-engine
  namespace: paysecure
spec:
  type: ClusterIP
  selector:
    app: settlement-engine
  ports:
  - port: 8082
    targetPort: 8082
    name: http
